<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_ingredient_import_form" model="ir.ui.view">
        <field name="name">recipe.ingredient.import.form</field>
        <field name="model">recipe.ingredient.import</field>
        <field name="arch" type="xml">
            <form string="Import Ingredients">
                <group invisible="state == 'done'">
                    <group>
                        <field name="file" filename="filename"/>
                        <field name="filename" invisible="1"/>
                        <field name="delimiter"/>
                    </group>
                    <group>
                        <div class="alert alert-info" role="alert">
                            <strong>CSV Format:</strong>
                            <br/>
                            Required columns: <code>name</code>, <code>price</code>, <code>uom</code>
                            <br/>
                            Optional columns: <code>code</code>, <code>category</code>, <code>supplier</code>, <code>date</code>
                            <br/><br/>
                            <strong>Example:</strong>
                            <pre>code,name,category,price,uom,supplier,date
VEG001,Tomato,Vegetables,2.50,kg,Local Farm,2026-01-13</pre>
                            <a href="/chefrulo_recipe/static/sample/ingredient_import_example.csv"
                               class="btn btn-link p-0">
                                Download sample CSV
                            </a>
                        </div>
                    </group>
                </group>
                <group invisible="state == 'draft'">
                    <field name="result_message" readonly="1" nolabel="1"/>
                </group>
                <field name="state" invisible="1"/>
                <footer>
                    <button string="Import" name="action_import" type="object"
                            class="btn-primary" invisible="state == 'done'"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_ingredient_import" model="ir.actions.act_window">
        <field name="name">Import Prices</field>
        <field name="res_model">recipe.ingredient.import</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
